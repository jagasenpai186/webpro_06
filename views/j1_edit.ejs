<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="utf-8">
  <title><%= data.team %></title>
</head>
<body>

  <h2><%= data.team %> クラブ情報の編集</h2>

  <!-- 更新フォーム -->
  <form action="/j1/update/<%= id %>" method="POST">
    <table border="1">
      <tr>
        <th>項目</th>
        <th>データ</th>
      </tr>

      <tr>
        <td>順位</td>
        <td><input type="text" name="rank" value="<%= data.rank %>"></td>
      </tr>

      <tr>
        <td>クラブ名</td>
        <td><input type="text" name="team" value="<%= data.team %>"></td>
      </tr>

      <tr>
        <td>勝点</td>
        <td><input type="text" name="points" value="<%= data.points %>"></td>
      </tr>

      <tr>
        <td>試合数</td>
        <td><input type="text" name="played" value="<%= data.played %>"></td>
      </tr>

      <tr>
        <td>勝</td>
        <td><input type="text" name="win" value="<%= data.win %>"></td>
      </tr>

      <tr>
        <td>分</td>
        <td><input type="text" name="draw" value="<%= data.draw %>"></td>
      </tr>

      <tr>
        <td>敗</td>
        <td><input type="text" name="lose" value="<%= data.lose %>"></td>
      </tr>

      <tr>
        <td>得点</td>
        <td><input type="text" name="goal" value="<%= data.goal %>"></td>
      </tr>

      <tr>
        <td>失点</td>
        <td><input type="text" name="against" value="<%= data.against %>"></td>
      </tr>

      <tr>
        <td>得失点差</td>
        <td><input type="text" name="diff" value="<%= data.diff %>"></td>
      </tr>

      <tr>
        <td>都道府県</td>
        <td><input type="text" name="prefecture" value="<%= data.prefecture %>"></td>
      </tr>

      <tr>
        <td>ホームスタジアム</td>
        <td><input type="text" name="stadium" value="<%= data.stadium %>"></td>
      </tr>

      <tr>
        <td>収容人数</td>
        <td><input type="text" name="capacity" value="<%= data.capacity %>"></td>
      </tr>
    </table>

    <br>
    <input type="submit" value="更新">
  </form>

  <br>

  <!-- 削除フォーム -->
  <form action="/j1/delete/<%= id %>" method="POST"
        onsubmit="return confirm('本当に削除しますか？');">
    <input type="submit" value="削除">
  </form>

  <br>
  <a href="/j1">キャンセル（順位表に戻る）</a>

</body>
</html>
